<h2 class="pt-4">Blog Posts</h2> 
<% @posts.each do |post| %>
	<div class="card p-4 m-4">
		<h3><%= post.title %></h3>
		<p><%= post.body %></p>
		<%= link_to("Read more", post_path(post) , :class => 'btn btn-light') %>
	</div>
<% end %>